{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Inventory Viewer",
            "url": "http://localhost:8000/index.html",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Serve"
        }
    ]
}